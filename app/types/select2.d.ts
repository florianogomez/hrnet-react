/**
 * TypeScript module declaration for the 'select2' jQuery plugin.
 *
 * This allows TypeScript to recognize imports of 'select2' in the project,
 * even if no type definitions are available for the library.
 */
declare module 'select2';
